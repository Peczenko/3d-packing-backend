plugins {
    // Auto-provisions the Java 25 toolchain (Gradle 8.14 itself runs on JDK 21).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'packing-backend'
