package com.packing.backend.infra;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Test-only composition root for {@code :infra} slice tests — see
 * {@code ApiTestApplication} for why each library module needs one of these.
 */
@SpringBootApplication
public class InfraTestApplication {
}
