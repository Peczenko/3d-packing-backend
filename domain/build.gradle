description = 'Pure domain model: aggregates, entities, value objects, domain events, invariants.'

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testImplementation 'org.assertj:assertj-core'
}
