## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] Target branch is `dev` (features) or `dev → master` (release)
- [ ] `./gradlew build` passes locally
- [ ] Bumped `version` in `build.gradle` (only for release PRs into `master`)
